# Infinite History

## How It Works

Scroll left on the chart to load older candles automatically. The platform loads historical data in batches as you scroll.

## Limitations

- Initial load: up to 2,000 candles (varies by plan)
- Each scroll loads 300 additional candles
- Total available history depends on the data source (Twelve Data or MT5)
